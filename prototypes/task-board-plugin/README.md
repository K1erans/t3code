# PROTOTYPE — task-board plugin, as its author would write it

Throwaway. Nothing runs; `sdk.d.ts` is an imagined SDK so the author code typechecks:

    tsc -p .

Question it answered: the "Write the task-board plugin as its author would" ticket in the
T3 plugin SDK v0 wayfinder map (K1erans/t3code#7). The ticket's resolution comment records
the reactions it produced. Open `DECIDE` comments are handed to later tickets.
